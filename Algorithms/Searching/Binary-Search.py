#taking input of values in array and searching for given value in array using binary search technique:
userIp=list(map(int,input().split()))
value=int(input())
def binary_search(lst,val):
    if len(lst)>1:
        mid=(start+end)//2
        while start<end:
            if mid
    return 0