#Taking user input list to sort the list using Insertion sort.
userIpLst=list(map(int,input().split()))
#Let's get started
for index in range(1,len(userIpLst)):
    for revindex in range(index):
        pass
